const verdadeiro = true;
//let tem escopo de bloco {} isso "{}" é um bloco
//var tem escopo de função
let nome = 'Luiz';
var nome2 = 'Luiz';
nome = 'Luiz' // isso pode
//let nome = 'joão' // isso não pode
if(verdadeiro) {
    let nome = 'Luiz'//isso pode
}