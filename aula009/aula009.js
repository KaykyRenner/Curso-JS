let NomeAluno; // undefined = não aponta local nenhum da memória
const SobreNomeAluno = null; // nulo = não aponta local nenhum da memória (usável)
const Aprovado = true; //Boolean = True, false (valor lógico)
console.log(typeof Aprovado)
const a =[1,2]
const b = a
// tipos de dados primitivos / String, number, boolean, undefined, null